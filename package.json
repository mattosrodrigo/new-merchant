{
  "name": "angular-merchant-demo",
  "version": "1.0.0",
  "description": "AngularJS Google Merchant Demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r app server.js package.json index.html .htaccess dist/"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
